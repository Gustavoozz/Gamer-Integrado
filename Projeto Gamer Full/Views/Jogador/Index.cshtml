@model Jogador

@{
    ViewData["Title"] = "Jogador";
}

<link rel="stylesheet" href="~/css/jogadores.css">

@if (ViewBag.UserName == null)
{
    <a class="navbar-brand" asp-area="" asp-controller="Login" asp-action="Index">Login</a>
}
else
{
    @* <!-- MAIN --> *@
    <main>
        <section class="container-grid cadastro-equipes">
            <form class="frm-cad-equipe" method="post" action='@Url.Action("Cadastrar")' enctype="multipart/form-data">
                <div class="cadequipe-titulo">
                    <h1>Cadastro de Jogadores</h1>
                    <hr>
                </div>
                <select name="IdEquipe" class="campo-entrada" id="equipe-select">
                    <option value="0">Selecione uma Equipe</option>
                    @foreach (var item in ViewBag.Equipe)
                    {
                        <option value="@item.IdEquipe">@item.Nome</option>
                    }
                </select>

                <input class="campo-entrada" type="text" name="Nome" id="nome-jogador" placeholder="Digite o nome do Jogador:" required>
                <input class="campo-entrada" type="email" name="Email" id="email-jogador" placeholder="Digite o Email do Jogador:" required>
                <input class="campo-entrada" type="password" name="Senha" id="senha" placeholder="Digite a Senha:" required>
                
                
                <button type="submit">Cadastrar</button>
            </form>


            @* <!-- TABELA CADASTRO DE EQUIPES --> *@

            <table>
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nome</th>
                        <th>Email</th>
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (Jogador jogador in ViewBag.Jogador)
                    {
                        <tr>
                            <td>@jogador.IdJogador</td>
                            <td>@jogador.Nome</td>
                            <td>@jogador.Email</td>
                            <td>
                            <img src="/img/Equipes/@jogador.Equipe.Imagem" alt="Imagem da equipe @jogador.Equipe.Nome" width="40px">
                            </td>
                           <td>
                                <a asp-area="" asp-controller="Equipe" asp-action="Excluir"
                                    asp-route-id="@jogador.IdJogador"><img src="~/assets/trash.png" alt="Excluir"></a>
                            </td>
                            <td>
                                <a asp-area="" asp-controller="Equipe" asp-action="Editar"
                                    asp-route-id="@jogador.IdJogador"><img src="" alt="Editar"></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </section>
    
    </main>    
}



@* 
<h1>Tela de Jogadores</h1>

@if (ViewBag.Username == null)
{
    <a class="navbar-brand" asp-area="" asp-controller="Login" asp-action="Index">Login</a>
}

else {
<form method="post" action="@Url.Action("Cadastrar")">

    <label for="">Nome</label>
    <input type="text" name="Nome">

    <label for="">Email</label>
    <input type="text" name="Email">

    <label for="">Senha</label>
    <input type="password" name="Senha">

  <label for="">Equipe</label>
        <select name="IdEquipe" id="IdEquipe">
            <option value="0">Selecione uma equipe: </option>
            @foreach (Equipe equipe in ViewBag.Equipe)
            {
                <option value="@equipe.IdEquipe">@equipe.Nome</option>
            }
        </select>


    <button type="submit">Cadastrar</button>
</form>



<table class="table table-striped table-responsive">
    <thead>
        <th>Nome</th>
        <th>E-mail</th>
        <th>Id de Jogador</th>
        <th>Imagem da equipe</th>
        <th>Excluir</th>
        <th>Editar</th>        
    </thead>

     <tbody>

        @foreach (Jogador jogador in ViewBag.Jogador)
        {
        <tr>
            <td>@jogador.Nome</td>
            <td>@jogador.Email</td>
            <td>@jogador.IdJogador</td>
            <td>
                <img src="/img/Equipes/@jogador.Equipe.Imagem" alt="Imagem da equipe @jogador.Equipe.Nome" width="50px">
            </td>
            <td>
                <a asp-area="" asp-controller="Jogador" asp-action="Excluir" asp-route-id="@jogador.IdJogador">Excluir</a>
            </td>
            <td>
                <a asp-area="" asp-controller="Jogador" asp-action="Editar" asp-route-id="@jogador.IdJogador">Editar</a>
            </td>
        </tr>
        }

    </tbody>
</table>
} *@  